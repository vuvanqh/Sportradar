

export type location = {
    locationId: string;
    venue?: string;
    city: string;
    country: string;
};

export type newLocation = {
    venue?: string;
    city: string;
    country: string;
}